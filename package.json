{
  "name": "android-logcat-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "dev:electron": "npm run engine:build && electron .",
    "build": "npm run engine:build && tsc --noEmit && tsc -p tsconfig.main.json && vite build",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "engine:build": "cargo build -p als-engine",
    "engine:test": "cargo test -p als-engine",
    "engine:run": "cargo run -p als-engine"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "electron": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-virtuoso": "latest",
    "zustand": "latest"
  },
  "devDependencies": {
    "@playwright/test": "latest",
    "@testing-library/jest-dom": "latest",
    "@testing-library/react": "latest",
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "typescript": "latest",
    "vite": "latest",
    "vitest": "latest"
  }
}
